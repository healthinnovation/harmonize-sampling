---
title: "Hierarchical clustering"
author: "X"
date: "`r Sys.Date()`"
output: html_document
---

```{r}
library(readr)
library(dplyr)
library(scales)
```

```{r}
db_raw <- read_csv("data/db_variables.csv")
```

```{r}
db <- db_raw %>% 
  dplyr::mutate(across(night_light:tmmn, ~ rescale(.x)))
```

